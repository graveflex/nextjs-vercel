import HeroSchema from './Hero';
import ImageWithContentSchema from './ImageWithContent';

// eslint-disable-next-line import/prefer-default-export
export const schemas = {
  HeroSchema,
  ImageWithContentSchema
};
