import { Block } from 'payload/types';

const Schema: Block = {
    slug: 'Layout',
    labels: {
        singular: 'Layout',
        plural: 'Layouts'
    },
    fields: []
}

export default Schema;

