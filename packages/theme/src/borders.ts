export const textInput = {
  color: 'secondary',
  radius: 0,
  style: 'solid',
  width: 1
};

export const textInputFocus = {
  ...textInput,
  color: 'primary'
};

const borders = {
  textInput,
  textInputFocus
};

export default borders;
